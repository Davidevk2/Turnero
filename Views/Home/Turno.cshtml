@{
    ViewData["Title"]="Asignacion";
}

<div class="container">
    <div class="text-center">
        <div class="card">
            <div class="card-body">
                <h1 class="card-titlee">@ViewBag.Turno</h1>
                <p class="card-text">@ViewBag.Usuario</p>
            </div>
            <div class="card-footer">
                <a asp-controller="Home"  asp-action="Ingreso" class="btn btn-lg btn-primary">Tomar turno</a>
            </div>
        </div>

    </div>
</div>