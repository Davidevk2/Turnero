@{
    ViewData["Title"]="Asignacion";
}

<div class="container">
    <div class="text-center">
        <div class="card">
            <div class="card-body">
                <h1 class="card-titlee">@ViewBag.Turno</h1>
                <p class="card-text">@ViewBag.Usuario</p>
            </div>
            <div class="card-footer">
                <button class="btn btn-lg btn-primary">Tomar turno</button>
            </div>
        </div>

    </div>
</div>